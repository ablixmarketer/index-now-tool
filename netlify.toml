[build]
  command = "npm run build"
  publish = "dist/spa"
  environment = { NODE_ENV = "production", VITE_API_BASE_URL = "https://index-now-tool.onrender.com" }

# SPA fallback - serve index.html for all non-API routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
